api:
  url: ${CONTRAST_URL}
  api_key: ${CONTRAST_API_KEY}
  service_key: ${CONTRAST_SERVICE_KEY}
  user_name: ${CONTRAST_USER_NAME}
agent:
  service:
    logger:
      path: contrast_service.log
      level: ERROR
    host: 127.0.0.1
    port: 30555
application:
  name: flaskr-with-contrast
